# Counterexample minimization

The minimizer attempts to return liquidity, volatility, latency, forced flow, and spread toward normal bounds only when the configured reproduction threshold remains violated. A retained adjacent scenario supports boundary inspection.
