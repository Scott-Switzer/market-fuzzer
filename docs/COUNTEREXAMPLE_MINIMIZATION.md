# Counterexample minimization

The minimizer attempts to return liquidity, volatility, latency, forced flow, and spread toward normal bounds only when the configured reproduction threshold remains violated. Every accepted step records before/after severity and seed failures. A retained adjacent scenario supports boundary inspection. “Minimal” means locally minimized within the declared search coordinates; it is not a proof of global minimality.
