# Product workflow

Market Fuzzer follows Strategy → Safety Properties → Baseline → Break My Strategy → Counterexample → Minimize → Fix and Retest → Regression Test. The deterministic product layer owns verdicts; AI is limited to structured, clearly labelled hypotheses and explanations.
