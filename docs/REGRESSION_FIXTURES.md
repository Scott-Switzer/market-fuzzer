# Regression fixtures

Exports include YAML and JSON with strategy, synthetic market parameters, event, assertions, expected result, seeds, command, and provenance. Run a YAML fixture with `smw test path/to/fixture.yaml`.
