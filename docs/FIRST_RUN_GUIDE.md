# First run

Choose **Start with POV example**, run the normal baseline, then select **Break My Strategy**. The intentionally fragile POV uses stale volume after latency stress, allowing the bounded search to demonstrate a participation violation. Export it, then compare the corrected POV implementation.
