# Render Python buildpack install list (mirrors pyproject.toml [project].dependencies).
# Render's buildpack installs from requirements.txt; the repo's requirements.txt
# is just `-e .` which does not install deps on Render — use this file for deploy.
fastapi>=0.115,<1
httpx>=0.27,<1
numpy>=2,<3
openai>=2.0,<3
pandas>=2.2,<4
pyarrow>=18,<25
pydantic>=2.10,<3
pyyaml>=6,<7
typer>=0.15,<1
uvicorn>=0.34,<1
# Tier 2 yfinance data source (used by the yfinance/historical run mode).
# Without it the deployed app silently reports "yfinance unavailable" and falls
# back to the synthetic fixture. Pinned loosely to avoid resolver surprises.
yfinance>=0.2,<1
